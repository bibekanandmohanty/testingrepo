import { DateTimeFilterPipe } from './date-time-filter.pipe';

describe('DateTimeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTimeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
